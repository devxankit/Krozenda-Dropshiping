// Seller-only permission keys. Kept identical to frontend's
// modules/seller/constants.js — the two apps must agree on these strings.
export const SELLER_PERMISSIONS = Object.freeze({
  ACCESS: 'seller.access',
})
