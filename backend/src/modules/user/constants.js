// User (buyer)-only permission keys. Kept identical to frontend's
// modules/user/constants.js — the two apps must agree on these strings.
export const USER_PERMISSIONS = Object.freeze({
  ACCESS: 'user.access',
})
