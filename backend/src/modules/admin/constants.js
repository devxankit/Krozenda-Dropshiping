// Admin-only permission keys. Kept identical to frontend's
// modules/admin/constants.js — the two apps must agree on these strings.
export const ADMIN_PERMISSIONS = Object.freeze({
  ACCESS: 'admin.access',
})
