// Partner-only permission keys. Kept identical to frontend's
// modules/dropshipping-partner/constants.js — the two apps must agree on
// these strings.
export const DROPSHIPPING_PARTNER_PERMISSIONS = Object.freeze({
  ACCESS: 'dropshipping_partner.access',
})
