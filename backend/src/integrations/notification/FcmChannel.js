import { NotificationChannel } from './NotificationChannel.js'

// Stub. Push notifications ONLY — do not extend this toward Firebase Auth,
// Firestore, Storage, or Remote Config (explicitly excluded, §5 / §4.3).
export class FcmChannel extends NotificationChannel {}
