import { PaymentProvider } from './PaymentProvider.js'

// Stub. The `razorpay` SDK is intentionally NOT installed yet — this
// scaffold is structure only (see project context §3.2 item 1: Razorpay
// Route must be confirmed provisioned on the client's account before this
// gets built out). Implement each method against the SDK once that's
// unblocked; nothing outside this file should need to change.
export class RazorpayProvider extends PaymentProvider {}
