import { LogisticsProvider } from './LogisticsProvider.js'

// Stub. Client-owned Shiprocket account + per-vendor pickup location setup
// is a prerequisite (project context §4.2, §10) — implement against the
// Shiprocket API once that's in place.
export class ShiprocketProvider extends LogisticsProvider {}
