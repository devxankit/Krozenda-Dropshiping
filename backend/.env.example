PORT=5000
ENV=development

MONGODB_URL=mongodb://localhost:27017/krozenda

JWT_SECRET=replace_with_a_long_random_string
JWT_ADMIN_SECRET=replace_with_a_long_random_string
JWT_VENDOR_SECRET=replace_with_a_long_random_string

BACKEND_URL=http://localhost:5000

# Comma-separated list of origins allowed by CORS in production
ALLOWED_ORIGINS=http://localhost:5173

# First super admin, auto-created on boot if no Admin document exists yet
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace_with_a_strong_password

# --- Payment gateway (prod-only, add once a provider is chosen) ---
# PAYMENT_KEY_ID=
# PAYMENT_KEY_SECRET=

# --- SMS / transactional notifications (prod-only) ---
# SMS_API_KEY=

# --- Shipping / logistics provider (prod-only) ---
# SHIPPING_API_KEY=
