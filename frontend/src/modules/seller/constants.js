// Seller-only constants. Shared vendor constants (status labels, etc.) live
// in modules/vendor-shared/constants.js — import from there, don't copy.

export const SELLER_PERMISSIONS = Object.freeze({
  ACCESS: 'seller.access',
})
