// Dropshipping-partner-only constants. Shared vendor constants (status
// labels, etc.) live in modules/vendor-shared/constants.js — import from
// there, don't copy.

export const DROPSHIPPING_PARTNER_PERMISSIONS = Object.freeze({
  ACCESS: 'dropshipping_partner.access',
})
