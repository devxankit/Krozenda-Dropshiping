// Module-local constants for auth. Cross-cutting constants (roles, order
// statuses, business models) live in src/config/constants.js.

export const AUTH_PERMISSIONS = Object.freeze({
  ACCESS: 'auth.access',
})
