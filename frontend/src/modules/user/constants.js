// Module-local constants for the buyer/user surface. Cross-cutting
// constants (roles, order statuses, business models) live in
// src/config/constants.js.

export const USER_PERMISSIONS = Object.freeze({
  ACCESS: 'user.access',
})
